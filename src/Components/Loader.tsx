import '../index.css'

export const Loader = () => {
  return (
    <section className="w-[100%] bg-white min-h-screen flex justify-center items-center">
        <span className="loader"></span>
    </section>
  )
}
